"""Nornyx governance A/B benchmark for CrewAI.

Modules are imported by path (``sys.path`` is extended in ``config``), not as a
package, so ``benchmark.py`` behaves the same whether it is run as a script or
imported by the test suite.
"""
