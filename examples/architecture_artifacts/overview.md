# Example Architecture

The API component depends on the governance component through a declared local interface. External tools check dependency direction and emit a bounded report for Nornyx to validate.
